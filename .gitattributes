# Keep the Composer dist archive to what an application needs. docs/ and examples/ ship: they are
# what the package is for. Tests and CI do not - they run from the repository.
/tests            export-ignore
/.github          export-ignore
/phpunit.xml      export-ignore
/phpstan.neon     export-ignore
/.gitattributes   export-ignore
/.gitignore       export-ignore
